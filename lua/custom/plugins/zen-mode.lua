return {
  "folke/zen-mode.nvim",
  opts = {
    window = {
      backdrop = 1,
      width = 80,
    },
  },
  -- TODO: make it :set wrap, :set linebreak, remap jk0^$ to prepend g for movement within broken lines.
}
